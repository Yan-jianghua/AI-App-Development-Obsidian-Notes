# `callable()`

## 作用

`callable()` 用来判断：

> 一个对象能不能被 `()` 调用。

语法：

```python
callable(对象)
```

返回：

```text
True
False
```

---

## 函数是 callable

```python
def test():
    pass


print(callable(test))
```

结果：

```text
True
```

因为：

```python
test()
```

是合法的。