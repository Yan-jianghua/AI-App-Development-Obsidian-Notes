# `isinstance()`

## 作用

判断一个对象是不是某个类的实例。

### 示例

```python
class Student:
    pass

s1 = Student()

print(isinstance(s1, Student))
```

结果：

```text
True
```

也可以：

```python
print(isinstance(10, int))
```

结果：

```text
True
```

### 一句话记忆

> `isinstance()` = 判断“这个对象是不是这个类型”