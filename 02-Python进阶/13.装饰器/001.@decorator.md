## `@decorator`

### 作用

`@decorator` 是装饰器的语法糖。

```python
@decorator
def test():
    print("hello")
```

等价于：

```python
def test():
    print("hello")

test = decorator(test)
```

也就是说：

> 把原函数传给装饰器，再把装饰后的函数重新赋值给原函数名。

### 记忆

```text
@decorator
↓
func = decorator(func)
```