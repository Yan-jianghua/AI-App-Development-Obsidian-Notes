# `delattr()`

## 作用

删除对象中的指定属性。

### 示例

```python
s1.age = 20

delattr(s1, "age")
```

相当于：

```python
del s1.age
```

删除后再访问：

```python
print(s1.age)
```

会报：

```text
AttributeError
```

### 一句话记忆

> `delattr()` = 根据名字删除属性。