# `hasattr()`

## 作用

判断对象是否拥有某个属性。

### 语法

```python
hasattr(对象, "属性名")
```

### 示例

```python
print(hasattr(s1, "name"))
```

结果：

```text
True
```

```python
print(hasattr(s1, "score"))
```

结果：

```text
False
```

### 一句话记忆

> `hasattr()` = 判断有没有这个属性。