# `setattr()`

## 作用

根据字符串形式的属性名**设置或修改属性**。

### 语法

```python
setattr(对象, "属性名", 值)
```

### 示例

```python
setattr(s1, "age", 20)
```

相当于：

```python
s1.age = 20
```

修改：

```python
setattr(s1, "name", "李四")
```

相当于：

```python
s1.name = "李四"
```

### 一句话记忆

> `setattr()` = 根据名字设置属性。