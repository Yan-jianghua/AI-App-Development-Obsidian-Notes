# `TypeError`

## 作用

使用了**不合适的数据类型**。

### 示例

```python
"年龄：" + 20
```

会报：

```text
TypeError
```

因为不能直接：

```text
str + int
```

### 另一个例子

```python
len(100)
```

也会报：

```text
TypeError
```

因为整数不能直接使用 `len()`。

### 一句话记忆

> `TypeError` = 数据类型用错了