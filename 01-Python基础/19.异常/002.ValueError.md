# `ValueError`

## 作用

数据类型本身没问题，但**值不合法**。

### 示例

```python
int("abc")
```

会报：

```text
ValueError
```

因为：

```text
"abc"
```

是字符串没错，但不能转换成整数。

### 处理

```python
try:
    age = int(input("请输入年龄："))
except ValueError:
    print("请输入正确的数字")
```

### 一句话记忆

> `ValueError` = 类型对，但值不对。