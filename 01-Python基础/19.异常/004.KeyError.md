# `KeyError`

## 作用

访问字典中**不存在的 key**。

### 示例

```python
student = {
    "name": "张三"
}

print(student["age"])
```

会报：

```text
KeyError
```

因为：

```python
"age"
```

不存在。

### 避免方法

可以使用：

```python
student.get("age")
```

这样不存在时不会报 `KeyError`。

### 一句话记忆

> `KeyError` = 字典里找不到这个键。