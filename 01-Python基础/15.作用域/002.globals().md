# `globals()`

### 作用

查看当前模块中的**全局变量**。

返回一个字典。

### 示例

```python
name = "张三"

print(globals())
```

里面会包含：

```python
"name": "张三"
```

也可以通过：

```python
globals()["name"]
```

获取全局变量。

### 一句话记忆

> `globals()` = 查看全局命名空间。