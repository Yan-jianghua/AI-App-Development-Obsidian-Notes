## `locals()`

### 作用

查看**当前局部作用域中的变量**。

返回一个字典。

### 示例

```python
def test():
    name = "张三"
    age = 20

    print(locals())

test()
```

输出类似：

```python
{
    "name": "张三",
    "age": 20
}
```

### 一句话记忆

> `locals()` = 查看当前局部变量。