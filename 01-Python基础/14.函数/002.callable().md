# `callable()`

## 作用

判断一个对象**能不能被调用**。

也就是能不能写：

```python
对象()
```

### 示例

```python
def test():
    pass

print(callable(test))
```

结果：

```text
True
```

普通数字：

```python
x = 10

print(callable(x))
```

结果：

```text
False
```

函数通常是可调用对象。

### 一句话记忆

> `callable()`：判断对象能不能加 `()` 调用。