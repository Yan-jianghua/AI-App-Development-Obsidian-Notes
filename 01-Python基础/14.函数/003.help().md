# `help()`

## 作用

查看对象的**帮助文档和说明**。

### 示例

```python
help(len)
```

可以查看：

- 函数作用
    
- 参数
    
- 返回值
    
- 使用说明
    

也可以查看自己写的函数：

```python
def add(a, b):
    """计算两个数字的和。"""
    return a + b

help(add)
```

### 一句话记忆

> `help()`：查看 Python 对象的使用说明。