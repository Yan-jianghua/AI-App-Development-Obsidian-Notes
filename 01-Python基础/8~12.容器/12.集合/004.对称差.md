# `^` 对称差

## 作用

找到两个集合中**不重复的部分**。

也就是：

> 只在 A 或只在 B 中存在，但不能同时存在。

```python
a = {1, 2, 3}
b = {3, 4, 5}

print(a ^ b)
```

结果：

```python
{1, 2, 4, 5}
```

共同存在的：

```python
3
```

被排除了。

### 一句话记忆

> `^`：双方不一样的部分