# `discard()`

## 作用

删除指定元素。

```python
a = {1, 2, 3}

a.discard(2)
```

和 `remove()` 最大区别：

如果元素不存在：

```python
a.discard(100)
```

**不会报错。**

### 对比

|方法|元素不存在|
|---|---|
|`remove()`|报错|
|`discard()`|不报错|

### 一句话记忆

> `discard()`：安全删除，不存在也没关系。