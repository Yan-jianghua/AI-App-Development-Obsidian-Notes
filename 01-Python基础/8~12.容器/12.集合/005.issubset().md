# `issubset()`

## 作用

判断一个集合是不是另一个集合的**子集**。

### 示例

```python
a = {1, 2}
b = {1, 2, 3, 4}

print(a.issubset(b))
```

结果：

```python
True
```

因为 `a` 中所有元素，`b` 都有。

可以理解为：

```text
a 是否完全包含在 b 里面？
```

### 一句话记忆

> `issubset()`：判断“我是不是被你完全包含”。