# `add()`

## 作用

向集合中**添加一个元素**。

### 语法

```python
集合.add(元素)
```

### 示例

```python
a = {1, 2}

a.add(3)

print(a)
```

结果：

```python
{1, 2, 3}
```

如果元素已经存在：

```python
a.add(2)
```

不会重复添加。

### 一句话记忆

> `add()`：添加一个元素