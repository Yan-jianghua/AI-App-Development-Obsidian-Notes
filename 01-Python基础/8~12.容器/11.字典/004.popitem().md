# `popitem()`

## 作用

删除并返回**最后加入字典的键值对**。

### 示例

```python
student = {
    "name": "张三",
    "age": 20,
    "score": 90
}

result = student.popitem()

print(result)
```

输出：

```python
("score", 90)
```

返回的是一个：

```text
(key, value)
```

形式的元组。

### 一句话记忆

> `popitem()`：删除最后一个键值对。