# `git remote`

## 查看远程仓库

```bash
# 列出已配置的远程名称。
git remote
# 列出远程名称及拉取、推送地址；不下载内容。
git remote -v
# 查看指定远程的地址、跟踪分支和推送信息。
git remote show origin
```

`origin` 只是默认常用名称，不是 Git 的关键字。

## 添加远程

```bash
# 添加远程仓库名称及地址；origin/upstream 只是常用名称。
git remote add origin https://github.com/OWNER/REPO.git
```

验证：

```bash
# 列出远程名称及拉取、推送地址；不下载内容。
git remote -v
```

## 修改 URL

```bash
# 修改已配置远程的地址，例如 HTTPS 改为 SSH。
git remote set-url origin git@github.com:OWNER/REPO.git
```

常用于 HTTPS 与 SSH 地址之间切换。

## 重命名和删除

```bash
# 重命名本地远程配置，不会重命名 GitHub 仓库。
git remote rename origin github
# 删除本地远程配置，不删除远程仓库。
git remote remove github
```

删除远程配置不会删除 GitHub 上的仓库，也不会删除已有本地提交。

## `remote origin already exists`

表示已经有名为 `origin` 的远程。先查看：

```bash
# 列出远程名称及拉取、推送地址；不下载内容。
git remote -v
```

如果 URL 错了，用 `set-url`，不要重复 `add`。

### 一句话记忆

> remote 是远程地址的本地别名，`origin` 只是最常用的那个名字。
