# 解决合并冲突

## 冲突为什么出现

两个分支修改同一文件的同一区域，Git 无法判断应保留哪一版：

```text
main 修改第 10 行
feature 也修改第 10 行
```

冲突不是 Git 损坏，而是需要人做业务决定。

## 冲突标记

```text
<<<<<<< HEAD
当前分支的内容
=======
被合并分支的内容
>>>>>>> feature/login
```

- `HEAD` 一侧是当前分支。
- 下方是正在合并进来的分支。

## 标准解决流程

1. 查看冲突文件：

```bash
git status
```

2. 打开文件，理解两侧意图。
3. 编辑出最终正确内容，删除全部冲突标记。
4. 运行测试或手动验证。
5. 标记已解决并提交：

```bash
git add <冲突文件>
git commit
```

## 放弃本次合并

```bash
git merge --abort
```

## 常见错误

- 不理解代码就选择 “Accept Current” 或 “Accept Incoming”。
- 留下 `<<<<<<<` 等标记后提交。
- 只让语法通过，没有验证业务行为。
- 冲突解决过程中又混入无关重构。

可以搜索残留标记：

```bash
git grep -n -e '<<<<<<<' -e '=======' -e '>>>>>>>'
```

### 一句话记忆

> 解决冲突是决定最终内容，不是机械选择其中一边。
